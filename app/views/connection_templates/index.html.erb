<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All connections
    </h1>

    <a href="/connections/new" class="btn btn-block btn-outline-secondary">
      Add a new connection
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "connection_templates/connections_search_form" %>
  </div>
</div>



<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Company
        </th>

        <th>
          Location
        </th>

        <th>
          Photo
        </th>

        <th>
          Name
        </th>

        <th>
          Linkein
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @connections.each do |connection| %>
      <tr>
        <td>
          <%= connection.id %>
        </td>

        <td>
          <% if connection.company.present? %>
            <a href="/companies/<%= connection.company.id %>">
              <%= connection.company.company %>
            </a>
          <% end %>
        </td>

        <td>
          <% if connection.location.present? %>
            <a href="/locations/<%= connection.location.id %>">
              <%= connection.location.address %>
            </a>
          <% end %>
        </td>

        <td>
          <img src='<%= connection.photo %>' class='img-fluid'>
        </td>

        <td>
          <%= connection.name %>
        </td>

        <td>
          <%= connection.linkein %>
        </td>

        <td>
          <%= time_ago_in_words(connection.created_at) %> ago
        </td>

        <td>
          <%= time_ago_in_words(connection.updated_at) %> ago
        </td>

        <td>
          <a href="/connections/<%= connection.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @connections, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
