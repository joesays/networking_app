<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Attendees

    <a data-toggle="collapse" href="#meeting_attendees_new_form">
      Add attendee
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @meeting.attendees.each do |attendee| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/attendees/<%= attendee.id %>">
          <%= attendee.connection_id %>
        </a>

        <a href="/delete_attendee_from_meeting/<%= attendee.id %>">
          Delete
        </a>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="meeting_attendees_new_form">
        <%= render partial: "meeting_templates/meeting_attendees_new_form" %>
      </div>
    </div>

  </ul>
</div>
